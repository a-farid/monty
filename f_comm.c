#include "monty.h"

/**
 * comments - do nothing.
 * @head: the head of the stack.
 * @nbLine: the number of line.
 * Return: no return.
 */
void comments(stack_t **head, unsigned int nbLine)
{
	(void) head;
	(void) nbLine;
}
